{
"$jsonSchema": {
    "bsonType": "object",
    "required": ["fragment_ids"],
    "properties": {
        "configuration_id": {
            "bsonType": "int",
            "description": "A unique ID that identifies what configuration was run. It links to a metadata document that defines all the settings that were used system-wide to generate this trajectory fragment"
            },
        "fragment_ids": {
            "bsonType": "array",
            "items": {
                "bsonType": ["string", "objectId"]
                },
            "description": "list of indices pointing to fragment IDs associated to current vehicle ID, sorted by start_timestamp"
            }
        }
    }
}

